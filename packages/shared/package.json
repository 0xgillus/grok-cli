{
  "name": "@grok-cli/shared",
  "version": "0.1.0",
  "description": "Shared utilities for grok-cli",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
